Set resolution in config.cfg. Default is configured for 800x480

Two libmylcd mylcd.dll builds are provided:
866KB: Compiled with non blocking updates enabled.
837KB: Compiled with blocking updates enabled.
Select build by renaming to mylcd.dll


Deform.exe:
Use keys:
T        Select next texture
Space    Cycles effect
Esc      Exit
